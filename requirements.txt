# Core data / numerics
numpy>=2.2
pandas>=2.1
scipy>=1.11

# Data source
yfinance>=0.2.40

# Technical indicators
# NOTE: pandas-ta on PyPI only ships versions requiring Python >=3.12
# (0.4.67b0+). This project's venv was upgraded to 3.12 to support it.
pandas-ta>=0.4.67b0

# Baselines
scikit-learn>=1.4
statsmodels>=0.14

# Deep learning (Phase 4)
# Apple deprecated tensorflow-macos; plain tensorflow now ships native arm64
# wheels (>=2.16). Add tensorflow-metal separately for optional GPU accel.
tensorflow>=2.16

# Plotting / EDA
matplotlib>=3.8
seaborn>=0.13

# Notebooks
jupyter>=1.0
ipykernel>=6.29

# Deployment (Phase 5)
streamlit>=1.35
plotly>=5.20

# Experiment tracking (Phase 2 of MLOps track)
mlflow>=3.0
